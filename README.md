# sober-machine
Maquina soberana

## Deployment recovery

Use `scripts/fix_failed_deployments.py` to clone failed Kilo deployment repositories, reproduce Docker build failures, and prepare conservative local fixes for review. See `docs/deployment_recovery.md` for target apps, debug commands, and benchmarking guidance.
